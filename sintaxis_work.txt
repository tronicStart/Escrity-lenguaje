La sintaxis que va a ver aqui por el momento no funciona por el momento
es solo para poder guiarse y saber como debe ser la sintaxis
Forma para declar variables
log variable_char = "dato"
log variable_int = 100
log variable_float = 3.1416

Forma de comentar el codigo
@Comentario

Forma de imprimir texto
text "Imprime un texto"
StringPrint "Imprime un texto"
Forma de resolver operaciónes arthmeticas
resolver(5*5)
operation(6*6)
5 * 5 / 25

Algunos operadores
++ Incremento
-- Decremento
+= Mas igual
-= Menos igual
< Mayor que
> Menor que
<= Mayor o igual que
=> Menor o igual que
== Igual
is Igual
= Asignación de una variable

Los bucles, condicionales y funciones
while true do
end

for log i = 0%i < 5%i++ do
end

if i is 6 do
end

function nombre_funcion ()
end

